{
  "name": "@hermes/gondolin-host",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Per-Hermes-session daemon that owns one Gondolin micro-VM and exposes JSON-RPC over a Unix socket.",
  "scripts": {
    "test": "node --test --test-reporter=spec test/"
  },
  "dependencies": {
    "@earendil-works/gondolin": "^0.12.0",
    "@msgpack/msgpack": "^3.1.3"
  }
}
